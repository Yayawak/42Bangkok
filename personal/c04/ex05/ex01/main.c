#include <stdio.h>
#include <string.h>
#include "ft_putstr.c"

int	main(void)
{
		char *str = " Create a function that displays a string of characters on the standard output.";
		ft_putstr(str);
	return (0);
}
