int	strlen(char *s)
{
	int	len;

	len = 0;
	while (*s != '\0')
	{
		len++;	
	}
	return (len);
}

char	*ft_strcat(char *dest, char *src)
{
	int	n, i;

	n = strlen(dest) + strlen(src);

	char	combine[n];
	while (i < n)
	{

	}

	return combine;
}
